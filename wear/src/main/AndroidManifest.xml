<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jonathan.inventoryassistant" >

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WearListenerService"
            android:enabled="true"
            android:exported="true" >
        </service>

        <activity
            android:name=".SelectGrp"
            android:label="@string/title_activity_select_grp" >
        </activity>
        <activity
            android:name=".ScanInItems"
            android:label="@string/title_activity_scan_in_items" >
        </activity>
        <activity
            android:name=".ItemScanConfirm"
            android:label="@string/title_activity_item_scan_confirm" >
        </activity>
    </application>

</manifest>
